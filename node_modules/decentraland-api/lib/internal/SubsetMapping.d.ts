export declare class SubsetMapping {
    oldValue: number;
    newValue: number;
    length: number;
    constructor(oldValue: number, newValue: number);
    contains(subset: SubsetMapping): boolean;
    toString(): string;
}
